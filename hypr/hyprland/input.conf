input {
    repeat_delay = 200
    repeat_rate = 50
    accel_profile = flat
    kb_layout = latam
    #kb_variant =
    #kb_model =
    #kb_options =
    #kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

